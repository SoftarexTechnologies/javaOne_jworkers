jar {
    manifest {
        attributes 'Implementation-Title': 'jWorker Demo Workers',
                   'Implementation-Version': version
    }
}

dependencies {
    compile project(':jworker-core')
    compile project(':jworker-demo-tasks')
}